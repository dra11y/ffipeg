name: ffipeg
description: Generate Dart FFI bindings to FFmpeg with a simple library annotation.
version: 0.1.1
repository: https://github.com/dra11y/ffipeg-dart

environment:
  sdk: ^3.5.3

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
